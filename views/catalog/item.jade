extends ../layout
block title
    | Конкурсы
block content
    header
        .container
            .page-title
                h2 Конкурсы
                p Конкурсы проводится в целях привлечения внимания общества к вопросам развития культуры, сохранения культурно-исторического наследия
    #block-detail.content-block
        .container
            .row
                .col-lg-9
                    h2= section.title
                    p.lead #{section.description}
                    hr
                    table.table.table-striped
                        thead
                            tr
                                th Название конкурса
                                th Даты&nbsp;проведения
                                th
                                th
                        tbody
                            each product in products
                                tr
                                    td: a(href="/product/#{product.url}")= product.title
                                    td
                                        if product.start
                                            |#{product.start}-#{product.end}
                                    td
                                        if product.price
                                            |#{product.price}&nbsp;руб.
                                    td: a(href="/product/#{product.url}", class="btn btn-success") Заявка
                .col-lg-3
                    .panel.panel-success
                        .panel-heading: h4 Блоки конкурсов
                        .list-group
                            each item in sections
                                a(href="/catalog/#{item.url}", class=(item.url == section.url)?'list-group-item active':'list-group-item') #{item.title}
